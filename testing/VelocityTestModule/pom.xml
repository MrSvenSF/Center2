<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>net.managerhub</groupId>
  <artifactId>mhcenter2-velocitytestmodule</artifactId>
  <version>1.0.1</version>
  <packaging>jar</packaging>

  <name>MHCenter2 Velocity TestModule</name>
  <description>MHCenter2 Velocity TestModule - proves that a proxy module is loaded and can register a command.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <mhcenter2.version>1.0.1</mhcenter2.version>
    <velocity.api.version>3.5.0-SNAPSHOT</velocity.api.version>
    <maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
    <maven.surefire.plugin.version>3.5.2</maven.surefire.plugin.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
    <!--
      The MHCenter2 module API. MHCenter2 is already running when the module is
      loaded, so these classes must never be packed into the module jar.
      Build the core once with "mvn install" in ../../MHCenter2.
    -->
    <dependency>
      <groupId>net.managerhub</groupId>
      <artifactId>mhcenter2</artifactId>
      <version>${mhcenter2.version}</version>
      <scope>provided</scope>
    </dependency>

    <!--
      Provided by the proxy. A module only needs this to name a Velocity event;
      everything else it uses comes from the MHCenter2 module API.
    -->
    <dependency>
      <groupId>com.velocitypowered</groupId>
      <artifactId>velocity-api</artifactId>
      <version>${velocity.api.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- The jar that is copied into plugins/MHCenter2/Modules/Jars on the proxy. -->
    <finalName>MHCenter2-VelocityTestModule-${project.version}</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
          <proc>none</proc>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven.surefire.plugin.version}</version>
      </plugin>
    </plugins>
  </build>

</project>
