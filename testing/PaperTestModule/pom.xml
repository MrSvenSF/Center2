<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>net.managerhub</groupId>
  <artifactId>center-testmodule</artifactId>
  <version>1.0.0-beta.1</version>
  <packaging>jar</packaging>

  <name>Center2 TestModule</name>
  <description>Center2 TestModule - proves that an external module jar is loaded.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <center.version>1.0.0-beta.1</center.version>
    <paper.api.version>1.21.11-R0.1-SNAPSHOT</paper.api.version>
    <maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
    <!--
      The Center2 module API. Center2 is already running when the module is
      loaded, so these classes must never be packed into the module jar.
      Build it once with "mvn install" in ../../Center2.
    -->
    <dependency>
      <groupId>net.managerhub</groupId>
      <artifactId>center</artifactId>
      <version>${center.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Provided by the Paper server, the module is PAPER only. -->
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>${paper.api.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- The jar that is copied into plugins/Center2/Modules/Jars. -->
    <finalName>Center2-TestModule-${project.version}</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
          <proc>none</proc>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
