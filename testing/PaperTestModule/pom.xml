<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>net.managerhub</groupId>
  <artifactId>mhcenter2-testmodule</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>MHCenter2 TestModule</name>
  <description>MHCenter2 TestModule - proves that an external module jar is loaded.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <mhcenter2.version>1.0.0</mhcenter2.version>
    <paper.api.version>1.21.11-R0.1-SNAPSHOT</paper.api.version>
    <maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
    <!--
      The MHCenter2 module API. MHCenter2 is already running when the module is
      loaded, so these classes must never be packed into the module jar.
      Build it once with "mvn install" in ../../MHCenter2.
    -->
    <dependency>
      <groupId>net.managerhub</groupId>
      <artifactId>mhcenter2</artifactId>
      <version>${mhcenter2.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Provided by the Paper server, the module is PAPER only. -->
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>${paper.api.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- The jar that is copied into plugins/MHCenter2/Modules/Jars. -->
    <finalName>MHCenter2-TestModule-${project.version}</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
          <proc>none</proc>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
