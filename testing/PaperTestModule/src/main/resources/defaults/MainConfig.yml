config-version: 1

enabled: true

# Was der Command antwortet. Nach einer Aenderung reicht "/center reload":
# das Modul liest diese Datei in onReload() neu ein und bleibt dabei laufen.
greeting: "MHCenter2 TestModule funktioniert."
